/**
 * @public
 */
export interface Options {}

/**
 *
 * return `ctablex` string
 *
 * @returns the `ctablex`  string
 *
 * @example
 * Here's an example:
 *
 * ```ts
 * import { ctablex } from '@ctablex/core'
 *
 * console.log(ctablex());
 * // Prints "ctablex":
 * ```
 */
export function ctablex(options?: Options) {
  return 'ctablex';
}
